services:
  portainer:
    image: portainer/portainer-ce:lts
    restart: always
    network_mode: service:tailscale
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer-data:/data

  tailscale:
    image: tailscale/tailscale:v1.90.8
    hostname: portainer
    secrets:
      - portainer_authkey
    environment:
      TS_STATE_DIR: /var/lib/tailscale
      TS_SERVE_CONFIG: /config/ts-serve.json
      TS_AUTHKEY: file:/run/secrets/portainer_authkey
    volumes:
      - portainer-ts-authkey:/var/lib/tailscale
      # Use a directory so `fsnotify` can watch the config file for changes
      - ${PWD}/ts-serve:/config
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    restart: unless-stopped

volumes:
  portainer-data:
  portainer-ts-authkey:


secrets:
  portainer_authkey:
    file: ${PWD}/../secrets/portainer.authkey
