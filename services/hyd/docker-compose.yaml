services:
  spirit-tracks:
    image: ghcr.io/ryandraves/spirit_tracks:latest
    network_mode: service:tailscale
    environment:
      SPIRIT_TRACKS_CONFIG: /config/secrets.json
      SPIRIT_TRACKS_SETEC_ADDR: https://setec.barn-arcturus.ts.net
    volumes:
      - hyd-secrets-hyd:/secrets/hyd
      - hyd-secrets-postgres:/secrets/postgres
      - ${PWD}/secrets.json:/config/secrets.json:ro
    restart: "no"

  hyd-db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: hyd
      PGUSER: hyd
      POSTGRES_DB: hyd
      POSTGRES_PASSWORD_FILE: /secrets/hyd_postgres_password
    volumes:
      - hyd-pgdata:/var/lib/postgresql/data
      - hyd-secrets-postgres:/secrets:ro
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "hyd" ]
      interval: 30s
      timeout: 20s
      retries: 3
    depends_on:
      spirit-tracks:
        condition: service_completed_successfully

  hyd:
    # bazel run //apps/hyd:hyd_amd64_load to build & load the image locally
    # bazel run //apps/hyd:hyd_push to push the image to the registry
    image: ghcr.io/ryandraves/hyd:latest
    restart: always
    network_mode: service:tailscale
    environment:
      POSTGRES_USER: hyd
      POSTGRES_DB: hyd
      POSTGRES_PASSWORD_FILE: /secrets/hyd_postgres_password
      POSTGRES_HOST: hyd-postgres
      POSTGRES_PORT: 5432
    volumes:
      - hyd-secrets-hyd:/secrets:ro
    depends_on:
      hyd-db:
        condition: service_healthy
      spirit-tracks:
        condition: service_completed_successfully

  tailscale:
    image: tailscale/tailscale:v1.90.8
    hostname: hyd
    secrets:
      - hyd_authkey
    environment:
      TS_STATE_DIR: /var/lib/tailscale
      TS_SERVE_CONFIG: /config/ts-serve.json
      TS_AUTHKEY: file:/run/secrets/hyd_authkey
    volumes:
      - hyd-ts-authkey:/var/lib/tailscale
      # Use a directory so `fsnotify` can watch the config file for changes
      - ${PWD}/ts-serve:/config
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    restart: unless-stopped

volumes:
  hyd-pgdata:
  hyd-ts-authkey:
  hyd-secrets-hyd:
  hyd-secrets-postgres:


secrets:
  hyd_authkey:
    file: ${PWD}/../secrets/hyd.authkey
