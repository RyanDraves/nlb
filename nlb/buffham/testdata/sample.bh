# A comment for fun
message Ping {
    # Add some comments here
    uint8_t ping;
}

message FlashPage {
    uint32_t address;
    list[uint8_t] data;  # What about some in-line comments?
    uint32_t read_size;
}

message LogMessage {
    string message;
}

message NestedMessage {
    uint8_t flag;
    LogMessage message;
    list[int32_t] numbers;
    Ping pong;
}


transaction ping[Ping, LogMessage];
transaction flash_page[FlashPage, FlashPage];
transaction read_flash_page[FlashPage, FlashPage];
